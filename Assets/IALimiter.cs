using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IALimiter : MonoBehaviour {
	[TextArea]
	public string info = "se direction for -1, a IA se direcionará para esquerda. Se direction for 1, a IA se direcionará para direita.";
	public int direction;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
